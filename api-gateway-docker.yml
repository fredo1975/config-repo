spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: filmotheque-service
          uri: lb://filmotheque-service
          predicates:
            - Path=/api/films/**
          filters:
            - StripPrefix=0
        - id: filmotheque-tmdb-service
          uri: lb://filmotheque-tmdb-service
          predicates:
            - Path=/api/tmdb/**

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
